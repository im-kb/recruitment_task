server:
  servlet:
    context-path: /company-task

logging:
  level:
    root: INFO

spring:
  datasource:
    url: 'jdbc:postgresql://localhost:5432/postgres'
    username: postgres
    password: password

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none

utils:
  personHeightValidator:
    minHeight: '160'